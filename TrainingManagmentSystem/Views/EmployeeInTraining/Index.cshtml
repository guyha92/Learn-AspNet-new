@model IEnumerable<TrainingManagmentSystem.Models.EmployeeInTraining>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>עובדים בהדרכה</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Training.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IfPass)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Training.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IfPass)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeInTrainingID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeInTrainingID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeInTrainingID })
        </td>
    </tr>
}

</table>
